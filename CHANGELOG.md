# Changelog

## 2026-07-17 — `[feat]` Временно скрыта система чеков по выплатам

Флаг `WITHDRAWAL_RECEIPTS_ENABLED` (default false = скрыто). При выключенном:
- чек-гейт больше НЕ блокирует вывод (отсутствие чека по прошлой выплате не мешает);
- `receipt_upload` и `admin_receipts` редиректят на дашборд;
- юзерская карточка «Чеки по выплатам» + админ-карточки «🧾 Чеки» скрыты.
Данные `receipt_*` сохранены. Вернуть = `WITHDRAWAL_RECEIPTS_ENABLED=true`.

Отдельно: по @nata_yu12 отменён approved-без-чека вывод #984 (4650₽, не выплачен)
→ возвращён в её легаси (10950 → 15600). Ещё 17 approved-без-чека на ~145 700 ₽
ждут сверки с Артёмом (кто реально не получал) — по решению владельца, поштучно.

## 2026-07-13 — `[feat]` Две реф-системы: % для аккредитованных, milestone для остальных

- **Аккредитованный рефовод** (галочка `is_accredited`) — обычные % с событий реферала
  (50 созвон / 1000 сделка).
- **НЕаккредитованный менеджер-рефовод** — процентов НЕ получает. Вместо них разовый
  бонус **500 ₽ за каждого реферала, приведшего 10 клиентов с /start в боте**.
  В кабинете — прогресс-бар по каждому рефералу (X/10, сколько осталось привести).
- **Реферал в обеих системах получает одинаково**: 100 за созвон / 3000 за сделку.
- Партнёры/админы под milestone НЕ подпадают — у них % всегда (аккредитация это
  пользовательская механика для вывода; на 2026-07-13 оба партнёра `is_accredited=False`,
  без этого исключения они потеряли бы свой процент).

Реализация: `lead_utils.is_milestone_referrer()` + `ref_started_clients_count()`;
`check_and_pay_subref_milestone()` переведён с «10 одобренных отчётов» (флоу мёртв)
на «10 клиентов с bot_started», идемпотентно по `subref_bonus_paid_at`. Порог
проверяется в вебхуке `/start` (само-восстанавливается на следующем старте).
`searchlink_sync`: `ref_gets_percent` — % только аккредитованному.

**Baseline (форвард-онли):** 39 рефералов с уже пройденным порогом помечены без
оплаты (иначе следующий /start выплатил бы бонус задним числом); 64 реферала
в процессе (1–9 клиентов) получат бонус при достижении 10.

## 2026-07-13 — `[fix]` Реф получает полные 3000 за сделку + ставки зафиксированы

**Баг «рефы получают 2900».** При сделке из выплаты вычитался аванс за созвон
(100 у рефа / 50 у рефовода) по флагу `sozvon_credited_at`. Но baseline проставлял
этот флаг БЕЗ оплаты (форвард-онли) — т.е. вычитали аванс, которого не было.

- Новый `_sozvon_actually_paid(link)` — источник истины `BalanceLog` (`sozvon#<link>`),
  а не baseline-флаг. Аванс был → сделка платит остаток (100+2900=3000); не был →
  платит полные 3000. **Итог всегда 3000 рефу / 4000 без рефовода.**
- **Доначислено 750 ₽** семерым за 5 сделок: @nata_yu12 +150, @aliya0593 +200,
  @Death88ze22 +100, @Zharenii02 +100, @ekaterina_kirillova4 +100, @Nastia051189 +50,
  @O9121986 +50. (@cherry9c не пострадала — её аванс реально платился.)

**Ставки зафиксированы, редактирование отключено.** Воронка берёт
`SEARCH_SOZVON_REFERRER`/`SEARCH_DEAL_REFERRER` из settings, а не поля юзера.
Rate-вьюхи → no-op/read-only, формы из шаблонов убраны, показ через
`_fixed_ref_rate_context()`. Двое успели накрутить себе ставку (100/2000 и 70/1500)
— сброшены к дефолту 50/1000, начислений по ним не было.

## 2026-07-13 — `[fix]` 500 при одобрении воркер-выводов + баннер «деньги на ожидании»

- **Фикс 500**: `admin_withdrawal_requests` падал при approve/reject/cancel воркер-заявок
  на вывод — `select_for_update().select_related("worker")` даёт Postgres-ошибку
  «FOR UPDATE cannot be applied to the nullable side of an outer join» (worker — nullable
  FK → LEFT JOIN). Добавлен `of=("self",)` (лочим только заявку). Обычные user-выводы
  (не-nullable FK, INNER JOIN) не были затронуты. Две зависшие заявки (@NataliyaKhmirova1,
  @qjenexkp по 2400₽) одобрены.
- **Баннер «Деньги на ожидании»** на дашборде главного админа: плюсовой основной баланс
  к выплате, легаси-баланс, активные заявки на вывод (сумма + кол-во).

## 2026-07-13 — `[feat]` Кабинет рефералов возвращён с редактированием ставок (под воронку)

Каждый рефовод снова задаёт свои доли с событий реферала — но теперь по новой
воронке (созвон/сделка), а не по старым отчётам. Вкладка была скрыта за
`LEGACY_REWARDS_ENABLED`; отвязали её от старых отчётов отдельным флагом.

- **Модель**: `User.ref_sozvon_cut` (default 50) + `ref_deal_cut` (default 1000),
  миграция `0091` (AddField-only, применена на прод). Реф получает `total - cut`.
- **Воронка**: `_apply_credit` читает индивидуальные доли рефовода
  (`referrer.ref_sozvon_cut/ref_deal_cut`, fallback на settings). Проверено
  rollback-тестом: созвон 150 при ставке 70 → менеджер 80, рефовод 70. Прод не менялся.
- **Гейт**: `@referral_system_required` + флаг `REFERRAL_SYSTEM_ENABLED` (default ON),
  независим от `LEGACY_REWARDS_ENABLED` — реф-кабинет виден, старые отчёты остаются off.
- **Заработок**: `_referral_earnings_breakdown` пересчитан из `BalanceLog`
  (`sozvon_ref#`/`deal_ref#` → SearchLink → реферал), а не из `PartnerEarning`.
- Вьюхи `partner_referrals`/`user_referrals`/`user_referral_list`/`partner_dashboard`
  + rate-вьюхи → созвон/сделка. Убран мёртвый sub-рефовод milestone (был на отчётах).
- Шаблоны: колонки Созвоны/Сделки, формы ставок (созвон из 150 / сделка из 4000),
  nav «Рефералы» для партнёров И менеджеров. Все страницы рендерятся 200.

## 2026-07-13 — `[fix]` VK в воронке SearchLink: матч по screen_name + аудит

Разбор жалобы «под VK не работает». **Итог: VK работает** — вебхук VK-бота
(`vk_handlers.py`) на каждый `/start` шлёт `vk_user_id`, ссылка получает
`bot_started=True` + `vk_user_id`, воронка матчит по `vk_id → telegram_users.vk_id`.
Из 15 сконвертившихся VK-клиентов (12 созвон + 3 сделки в windowgram) 12 корректно
привязаны к ссылкам на верной стадии; 3 — органика без SearchLink. Оба сегодняшних
VK-старта записались правильно. Причина «мало VK» — низкий трафик: 12 созвон + 3 сделки
против TG 796 + 282, а не баг.

- **Фикс `_fetch_wg_state`**: добавлен fallback `vk_screen_name → telegram_users.username`
  (строго `platform='vk'`). Раньше матчили VK только по числовому `vk_user_id`, и 353
  ссылки с одним лишь `screen_name` (ручная привязка по тегу / старый вебхук) не
  матчились вообще. Теперь приоритет: `telegram_id → username → vk_user_id → vk_screen_name`.
  Восстановлено 14 ссылок (регрессии по TG/vk_id нет: 186/200 и 174/200).
- Созвон-статусы оставлены строго `/жду` + `/ответ` (по решению) — статусы `/согласовано`,
  `/пушить`, «просроченная дата» за созвон НЕ считаем (87 клиентов остаются на стадии «чат»).

## 2026-07-13 — `[feat]` Отключение старой реф-системы UI (Фаза 2, неразрушающе)

Старая партнёрско-affiliate реф-система выводится из эксплуатации. **Данные и связи
`partner_owner` сохранены** (ничего не дропаем) — новая воронка их использует.
Перед изменениями снят полный бекап прода (`backup_prod_20260713.dump`, 21 МБ).

- Флаг `LEGACY_REWARDS_ENABLED` (default false).
- Скрыты точки входа: nav «Рефералы» (partner), карточка «Реферальная система» на дашборде.
- Гард `@legacy_rewards_required` на кабинеты/настройки ставок: `partner_referrals`,
  `user_referrals`, `partner_ref_rates`, `user_referral_list`, `partner_update_rates`,
  `user_update_ref_rates` → редирект на дашборд.
- Реф-регистрация (`/a/<code>/`, `/p/<code>/`) НЕ трогается — новые рефералы по-прежнему
  привязываются к рефоводам (partner_owner), начисления идут через новую воронку.
- **Отчёты по группам сняты**: карточка «Отчёты по группам» на дашборде менеджера и
  админ-карточки «Группы — модерация»/«Права на отчёты» скрыты за `legacy_rewards_enabled`;
  подача `manager_group_report_create` → редирект. Данные сохранены.
- **Старый крединг остановлен**: approve-флоу `admin_lead_approve`, `admin_group_report_approve`,
  `admin_search_report_approve` заблокированы при выключенном `LEGACY_REWARDS_ENABLED` →
  начисляем ТОЛЬКО через новую воронку windowgram. reject/rework оставлены (корректировки, не начисляют).
  Pending Lead/GroupReport по решению НЕ рассчитываем (за групрепорт менеджер получает только по статусу).

## 2026-07-13 — `[feat]` SearchLink 2.0: воронка windowgram + авто-начисления (Фаза 1)

Новая система SearchLink: начисления менеджеру идут автоматически по статусу
клиента в CRM windowgram, без отчётов.

**Воронка** (поля на `SearchLink`, миграция 0090): `funnel_stage` 1 бот → 2 чат →
3 созвон → 4 сделка; `sozvon_credited_at` / `deal_credited_at` (идемпотентность).

**Синхронизатор** `core/searchlink_sync.py` (в почасовом cron `zvonok_poll_cron`):
- Матч клиент↔ссылка по telegram_id/username/vk_id (кросс-DB к windowgram).
- Стадии: чат = `group_chat_id`; созвон = `status ∈ {waiting_payment, waiting_no_date, answer_date}`; сделка = `status='paid'`.
- Начисления (идемпотентно): созвон всего 150 (с рефоводом 100/50; без — 150 + Варваре 10);
  сделка всего 4000 за вычетом созвона (с рефоводом 3000/1000; без — 4000 + Варваре 100).
- **Форвард-онли**: baseline пометил текущих созвон/сделку-клиентов как обработанных
  без выплаты (303 созвон + 73 сделки), платим только за новые переходы.

**Вкладки «Мои ссылки»**: вместо отчётных — воронка (Все / Запустил бота / Создан
чат / Проведён созвон / Успешная сделка). Колонка «Стадия» вместо Контакт/Вложение/Статус.

Фаза 1 из 3. Дальше: снос старой реф-системы (PartnerLink/PartnerEarning/*_cut) и
ретайр Lead/GroupReport-отчётов.

## 2026-07-13 — `[feat]` SearchLink вернули менеджерам — без отчётов (переходный режим)

Менеджеры снова могут создавать SearchLink-ссылки и проверять старт бота
клиентом + видят всю старую статистику. Но подача отчётов и оплата пока
выключены (готовится новая система).

- Новый флаг `SEARCHLINK_REPORTS_ENABLED` (по умолч. **false**); `SEARCHLINK_ENABLED`
  теперь по умолч. **true**.
- Менеджеры (`SEARCHLINK_ENABLED`): «Мои ссылки», создание ссылок, вкладки-статистика,
  проверка `bot_started` — вернулись. Карточка SearchLink на дашборде снова видна.
- Отчёты (`SEARCHLINK_REPORTS_ENABLED=false`): `search_report_create`/`redo` →
  редирект на «Мои ссылки»; кнопки «Отчёт»/«Доработать»/«Изменить» скрыты; на
  дашборде и «Моих ссылках» — пояснение, что приём отчётов приостановлен.
- Админ: карточка проверки отчётов остаётся в «🗄 Легаси», пока reports off
  (новых отчётов не поступает). Гейт админ-раздела переключён на `searchlink_reports_enabled`.

## 2026-07-12 — `[feat]` Легаси-баланс: заморозка балансов менеджеров

Старая система замораживается, готовится новая. Текущие балансы обычных
менеджеров (`role=user`) фиксируются в новом поле `User.legacy_balance`.

- Миграция **0089** — добавляет `User.legacy_balance` (IntegerField, default 0).
- **Перенос** (разовая операция на проде): у менеджеров с **положительным**
  балансом `legacy_balance = balance`, затем `balance = 0`. С логами в BalanceLog.
- **Минусовые балансы НЕ трогаем** — остаются на основном балансе (по решению).
- **Отмена выводов**: все pending-заявки менеджеров (18 шт., 96 055 ₽) отменены
  (`status=rejected`), сумма добавлена в `legacy_balance` (не на основной баланс).
  Деньги вывода уже были списаны при подаче — так они тоже замораживаются.
- Итог: `legacy_balance` = 312 030 ₽ (215 975 заморозка + 96 055 отменённые выводы).
- Вывод с легаси-баланса пока недоступен (выплатим позже).
- UI: строка «Легаси: N ₽» на дашборде менеджера и на админ-странице баланса.

## 2026-07-12 — `[feat]` SearchLink выведён в легаси + одобрены отчёты из очереди

Отжившая система SearchLink. Приём отчётов закрыт, код сохранён —
вернуть можно `SEARCHLINK_ENABLED=true`.

**Одобрение очереди** (разовая операция на проде):
- Одобрены **48 отчётов** из очереди проверки Вики (ровно `pending`, видимые:
  `bot_started=True`/подтверждённый звонок, не дубли, не накрутка).
- Начислено менеджерам 5810 ₽ + рефоводам (cut) 1390 ₽ = 7200 ₽ (48×150),
  Вике (varvara_lead) +480 ₽ за проверку (48 SearchReportReviewLog).
- Логика 1:1 с `admin_search_report_approve` (реф-сплит, milestone, paid_reward).
- Исключены: 94 rework (отбиты назад), 4 pending_callback, ~3500 без `bot_started`.

**Флаг `SEARCHLINK_ENABLED`** (default false):
- Юзерам: карточка на дашборде скрыта; `@searchlink_required` на вьюхах
  `search_links_my`, `search_link_create`, `search_report_create`,
  `search_report_redo` → редирект. Заливать отчёты нельзя.
- Админам: карточка SearchLink уезжает в секцию **«🗄 Легаси»** на дашбордах;
  очередь проверки (`admin_search_reports_list`) остаётся для разбора истории.

## 2026-07-12 — `[feat]` Отдел дожима скрыт на проде (обратимо) + консолидация баланса

Отдел дожима выведен из эксплуатации. Код сохранён целиком — вернуть можно
переменной окружения `DOZHIM_ENABLED=true`.

**Фиче-флаг** (`base_site/settings.py`):
- `DOZHIM_ENABLED = os.getenv("DOZHIM_ENABLED", "false") == "true"` — по умолчанию скрыт.

**Скрытие UI** (флаг инжектится глобально через `department_context`):
- Контекст-процессор форсит `department="search"` когда флаг выключен → вся
  дожим-навигация/карточки на дашборде юзера авто-скрываются.
- `base.html`: переключатель отделов «Поиск/Дожим» скрыт (`and dozhim_enabled`).
- Юзер-вьюхи дожима (6) и партнёрские (5) — декоратор `@dozhim_required` (редирект).
- `switch_department` и `request_withdrawal_create` форсят `dept="search"`.
- Админ проверка отчётов (`admin_leads_all_new`): дожим-лиды исключены из выборки
  и фильтр отделов скрыт. Дожим-карточки на админ-дашбордах, колонки/бейджи баланса
  дожима в таблицах — под `{% if dozhim_enabled %}`.
- Финансовый отчёт (`admin_user_finance_report`) оставлен как есть — это аудит-инструмент.

**Консолидация баланса** (разовая операция на проде):
- 34 открытых дожим-лида (24 pending + 10 rework) одобрены (reviewed_by=varvara_lead),
  юзерам начислено +1360 ₽, Вике (balance_admin) +85 ₽ за проверку (34 LeadReviewLog).
- Весь `dozhim_balance` перенесён на `balance` (5660 ₽ у 24 юзеров), с логами в BalanceLog.
- `dozhim_balance` обнулён у всех.

## 2026-05-26 — `[feat]` Завод-лидов: обмен номерами клиент ↔ главный админ

Новый кабинет для роли `lid_customer`. Юзер `zavodlidov`/`1111` создаётся миграцией 0081.

**Клиент** (`/zavod/`):
- Вставляет номера в textarea (любые разделители: запятая, пробел, новая строка).
- Видит историю по дням: «мои номера / ответ админа / статус».
- После закрытия дня (11:00 МСК) — кнопка «Скачать Excel».

**Главный админ** (`/staff/zavod-lidov/` + карточка на дашборде):
- Видит всех клиентов с подсветкой тех, кто ждёт ответ.
- На каждого — отдельный textarea для вставки своих номеров.
- Бейдж на дашборде показывает сколько клиентов ждут.

**Парование** (детерминистично при скачивании):
- `rows = max(len(user), len(admin))`
- Все номера каждой стороны представлены хотя бы раз
- Разница добивается случайным повторением (`random.choices(seed=date)`)
- Один файл `.xlsx`, по листу на каждый закрытый день
- Повторное скачивание даёт идентичный файл

**Архитектурно:**
- Бизнес-дата (cutoff 11:00 MSK) считается на лету в `business_date_now()` — никакого cron.
- Дубликаты внутри одного (customer, date, side) отсекаются БД через UniqueConstraint.
- На прода юзер `zavodlidov` создаётся data-миграцией, status=approved.

## 2026-05-21 (утро) — `[feat]` Админы получают реф-ссылку + расширенный API кураторов

**Task 1: Реф-ссылка для админов.** Раньше реф-система была доступна только `role=user` (approved). Расширил `_require_user_approved` — теперь `admin` и `main_admin` тоже могут приглашать рефералов и получать % с одобренных отчётов. На дашбордах админа и главного админа появилась карточка **«🤝 Реф-ссылка»** → переход на `/referrals/`.

Approve-логика Lead/SR/GR не требовала правок: ELSE-ветка (после `is_subreferrer` и `role=partner`) уже корректно начисляет `PartnerEarning` любой роли через стандартные поля `User.ref_lead_reward / ref_searchlink_cut / ref_group_report_cut`. Админ настраивает ставки в `/referrals/` как и обычный user-рефовод.

**Task 2: API кураторов с заработком.** В `GET /api/curators/` добавил два поля по каждому куратору:
- `referrals_count` — сколько рефералов у привязанного site-аккаунта куратора
- `earned_total_rub` — суммарный заработок куратора с этих рефералов (`Sum PartnerEarning where partner=account`)

Bulk-агрегаты, без N+1.

Новый эндпоинт `GET /api/curators/<id>/referrals/` — детально про каждого реферала конкретного куратора: `username, days_on_platform, leads_total (approved Lead+SR+GR), earned_for_curator_rub`. Для CRM-карточки куратора.

Все эндпоинты на Bearer `SEARCH_BOT_WEBHOOK_SECRET`.

## 2026-05-21 (под утро) — `[fix]` GroupReport approve/reject/rework — идемпотентны

Жалоба: «одобряем отчёт в "Не полные", не пропадает из списка». Повторно — «уже одобрен».

Причина: при повторном клике (двойной клик / устаревший DOM / несколько вкладок) сервер возвращал `success=false` с сообщением «уже одобрен», и JS-обработчик уходил в else-ветку → красный toast и **строку не удалял**. С точки зрения юзера — «не пропадает».

Фикс: все три action'а идемпотентны на бэке.
- approve: если уже APPROVED → `success=true`, «уже был одобрен».
- reject: если уже REJECTED → `success=true`, «уже отклонён».
- rework: если уже REWORK → `success=true`, «уже на доработке».

Фронт всегда получает success → `fadeRow` → строка точно пропадает.

## 2026-05-21 (поздняя ночь) — `[ui]` Убрал вкладку «Дубликаты» из SearchReports

Дубликаты и так не должны появляться в списках. Проверил: они уже автоматически:
- Создаются с `status=rejected` при сабмите (`_find_duplicate_search_report`).
- Имеют `duplicate_of_id != NULL`.
- Все обычные вкладки фильтруют `duplicate_of__isnull=True` — то есть дубликаты в них не попадали.

Единственное место где они светились — отдельная вкладка «Дубликаты» с бейджем 51. Убрал её из views + шаблона + dead-code бейдж DUP. На проде 51 дубликат уходит из UI (в БД остаются как rejected для форензики).

## 2026-05-21 (ночь) — `[feat+ux]` API статистики юзеров для CRM + AJAX-одобрение GR

**Task 1 — новый API для windowgram:**

`GET /api/users-stats/` отдаёт JSON-список пользователей с агрегированной статистикой одобренных отчётов. Авторизация: `Bearer SEARCH_BOT_WEBHOOK_SECRET`. Параметры: `?role=user` (default) или `all`, `?limit`, `?offset`.

Каждый юзер: `id, username, role, status, date_joined, days_on_platform, leads_total, leads_today, leads_week`. «Лиды» = approved Lead + SearchReport + GroupReport (любого типа). Bulk-агрегаты: 9 запросов (3 таблицы × 3 периода) на весь список, масштабируется на 2k+ юзеров.

На проде total=2335 юзеров. Пример: `@Marinka_1998 — 2593 leads_total за всё время`.

**Task 2 — UX-фикс одобрения GroupReport:**

Жалоба: «при одобрении страница обновляется». На самом деле approve уже был AJAX, но кнопки ✎ (rework) и ✗ (reject) вели на отдельные страницы с формой → submit → redirect. Это и воспринималось как «страница обновляется».

- Reject/rework на бэке теперь принимают AJAX-POST с простым `reason`/`comment` (без ModelForm). Возвращают JSON `{success, message}`. HTML-флоу с формой оставлен на старых URL для прямых заходов.
- В шаблоне `<a href>` → `<button>` + два modals (Reject, Rework) + JS-обработчики аналогичные SearchReport. Toast-нотификации.
- При успехе строка fadeRow + декрементим бейдж активной вкладки. UI-состояние обновляется без F5.

## 2026-05-21 (поздний вечер) — `[refactor]` Кураторы: 1 куратор ↔ 1 аккаунт на сайте

Изначально я неверно прочитал модель: сделал «куратор → много привязанных подопечных». На самом деле у куратора подопечные только в TG; на нашем сайте надо привязать **сам аккаунт куратора** (1:1).

- `User.curator` (FK user→curator) удалён.
- `Curator.account` (OneToOne→User) добавлен.
- Detail-страница: action'ы переименованы (`set_account` / `clear_account`). Поиск теперь по любым юзерам, не только role=user. Если аккаунт уже занят другим куратором — внятная ошибка.
- В списке колонка «Юзеров» → «Аккаунт на сайте» (зелёный `@username` или оранжевый «не привязан»).
- API `/api/curators/`: `users_count` → `account_username` + `account_id`. Windowgram-прокси и HTML-страница обновлены, сервис перезапущен.
- Миграция 0080 безопасная (на проде 0 привязок).

## 2026-05-21 (вечер) — `[feat]` Кураторы: синхронизация с CRM (windowgram)

В CRM-системе (windowgram, бот-сервер в NL) появился доступ к списку кураторов с partnerka. Источник правды — partnerka, CRM read-only зеркало.

**На partnerka:**
- `GET /api/curators/` — JSON-список кураторов. Авторизация: `Bearer SEARCH_BOT_WEBHOOK_SECRET` (тот же ключ что у search-bot-start webhook и `/api/admins/external`).
- Поля: `id, tg_username, display_name, is_active, users_count, created_at`.
- Опциональный фильтр `?active=1`.

**На windowgram** (`/opt/windowgram/backend/app/api/curators.py`):
- `GET /api/curators` — JSON-прокси к partnerka. Авторизация: текущий CRM-юзер.
- `GET /api/curators-page` — HTML-страница со списком (для случая, когда SPA-вкладку не получается добавить — фронт собран без исходников на сервере). Открывается админом по прямой ссылке: `https://murzzvon.ru/api/curators-page`.

Сервис `windowgram` перезапущен. Эндпоинты подтверждены: partnerka API → 200 (`ok=true, count=0`), windowgram local → 403 без auth (правильно).

## 2026-05-21 (день) — `[feat]` Кураторы: первая итерация

Новая сущность `Curator` — внешние тимлиды, ведущие группу исполнителей в TG. Главный админ на дашборде видит карточку «🧑‍🏫 Кураторы» (жёлтая, с каунтером активных).

На странице `/staff/curators/`:
- Список всех кураторов с их TG-никами, именами (опционально), счётчиком привязанных юзеров и статусом (активен/архив).
- Форма создания: `@tg_username` + опционально имя. При вводе `@` или `t.me/...` URL автоматически нормализуется.
- Кнопка «В архив / Восстановить» на каждой строке (soft-disable вместо удаления).

На странице `/staff/curators/<id>/`:
- Редактирование `tg_username` + имени.
- Поиск пользователей сайта по `@username` для привязки. Показываются только `role=user`, не привязанные к этому куратору; если уже у кого-то — в списке видно, что это смена куратора.
- Список привязанных юзеров с кнопкой отвязки.

Связь: `User.curator` — nullable FK на `Curator`, `on_delete=SET_NULL`. Один юзер ↔ один куратор.

Это первый шаг — дальше с кураторами будет работа (статистика, выплаты, рассылки).

## 2026-05-21 — `[feat]` Регистрация: обязательный @ник Telegram + валидация формата

Логин при регистрации = TG @username. Это единственный способ связи с исполнителем у команды.

- Label: «Ваш @ник в Telegram», placeholder `@your_nickname`, help-text прямо говорит «без него заявку не примут».
- `clean_username` снимает префиксы `@`, `https://t.me/`, `t.me/`, `telegram.me/` и trailing `/` — то есть менеджер может вставить ссылку «https://t.me/ivan» и она превратится в `ivan`.
- Regex по официальным правилам TG (`(?!.*__)[a-zA-Z][a-zA-Z0-9_]{3,30}[a-zA-Z0-9]`): 5–32 символа, латиница/цифры/_, не начинается с цифры или `_`, не оканчивается на `_`, без подряд идущих `__`.
- Дружелюбная ошибка с примером.
- Case-insensitive uniqueness check (без него `IvanP` и `ivanp` считались бы разными — потенциальный фрод).

Существующие 2393 юзера не затронуты — clean_username вызывается только при регистрации. Логиниться смогут все, включая legacy с короткими/нелатинскими/телефонными ником.

## 2026-05-20 (вечер) — `[fix]` GroupReport «Не полные»: уже одобренные не пропадали

Жалоба от Виктории: «одобряем отчёт, но он не пропадает из списка. При повторном клике — "уже одобрен"».

Причина: фильтр вкладки `incomplete` был `is_complete=False` без учёта статуса. После approve флаг авто-валидации не меняется (отчёт как был «не полным» — так и остался), статус становится approved — но запись продолжала светиться в списке. На проде в `is_complete=False` было 163 pending + **38 approved** «зомби», а counter (163) был правильным → list и counter рассинхронизировались.

Фикс: `qs.filter(is_complete=False, status__in=[PENDING, REWORK])`. Теперь вкладка зеркалит логику «Новые» — показывает только то что реально на проверке. Одобренные/отклонённые → в свои штатные вкладки.

## 2026-05-20 (день) — `[perf]` Дашборд главного админа: ~5× меньше запросов

Жалоба «у главного админа лагает сайт, у меня нет». Профилировал — нашёл узкое место в `admin_stats_list` блоке дашборда:

- Цикл по 8 staff юзерам.
- На каждого `admin/main_admin` вызывались `total_actions()` + `total_earned()`, каждая делала 3 COUNT (Lead+SR+GR review logs). 6 одинаковых COUNT на юзера, 18 на 3 админов.
- Для balance_admin внутри цикла **повторно** гонялся expensive JOIN-count `LeadReviewLog.filter(action=approved, lead__user__partner_owner__isnull=True)` на 53 695 строках.
- Итого ~40 COUNT'ов на одну загрузку.

Фикс:
- Новая функция `actions_earned_for_admins(ids)` в `admin_earnings.py` — один `GROUP BY admin_id` на каждую review-log таблицу = 3 запроса на ВСЕХ админов сразу.
- `withdrawn_by_user`, `pe_by_partner`, `refs_by_owner` — bulk-агрегаты, по 1 запросу.
- `ba_total/ba_sr` для balance_admin'а вычисляются один раз перед циклом.

Стало ~8 запросов вместо ~40. На 53к LeadReviewLog ускорение заметное.

Совместимость: `total_actions/total_earned/count_lead_actions/...` остались — их используют другие места (context_processors, earnings stats). Проверил численно: bulk возвращает идентичные значения per-user функциям.

## 2026-05-20 — `[fix]` 1-я выплата без чека = `waived` навсегда

Симптом: менеджер сделал свой 1-й вывод (по правилу «первый без чека»), его одобрили, а при попытке 2-го вывода получил блок «загрузите чек по предыдущей выплате». То есть «1-й без чека» работало только на момент СОЗДАНИЯ заявки, но не освобождало от чека НАВСЕГДА.

Причина: при approve `receipt_status` оставался `'none'`, и при следующем выводе гейт находил эту 1-ю и блокировал. Юзер должен был загрузить чек задним числом — хотя по правилу не должен.

Фикс:
- При approve проверяем, есть ли у юзера ДРУГИЕ approved-выплаты. Если нет — это 1-я → ставим `receipt_status='waived'`. Дублируем в одиночном и bulk-approve.
- **Backfill на проде**: для каждого юзера помечал самую старую `approved` как `waived`, если была `none`. Разблокировано 7 пользователей (Marinka_1998, leliyaaaa, Nastia051189 и др. с единственным одобренным выводом).

## 2026-05-19 (ночь) — `[feat]` GroupReport: incomplete-отчёты доступны всем модераторам

Раньше вкладка «⚠ Не полные» (`is_complete=False` — не прошли авто-валидацию против `windowgram.admin_task_progress`) видна была только `main_admin`. Approve/reject/rework тоже блокировались гейтом `is_main_admin`. В итоге у обычных модераторов копились неразобранные отчёты — на проде сейчас **189 pending в incomplete**.

- Снял ограничения в списке (вкладка + каунтер видны всем admin/main_admin), в просмотре вложения, в approve/reject/rework.
- Ставка не меняется — тот же `GROUP_REPORT_APPROVE_REWARD = 80 ₽` через существующий `_split_group_report_payout`.
- Гранты прав на создание отчётов остаются `main_admin only` (это не модерация).
- В шаблоне: бейдж вкладки сделал жёлтым (чтобы отличать), подсказка «проверять вручнее, читай `validation_note`».

## 2026-05-19 (ночь) — `[fix]` Заявки на вывод: «выбрать все» и bulk-действия не работали

На `/staff/withdrawal-requests/` чекбокс «выбрать все» в шапке не реагировал, и bulk-кнопки «✓ Одобрить» / «✗ Отклонить» не отправляли форму.

Причина: невалидный HTML — внешние `<form id="bulkFormRegular/Ref">` оборачивали карточки, а внутри строк лежали отдельные `<form>` для одиночных approve/reject. Браузер игнорирует вложенный `<form>` тэг, и в итоге кнопки/чекбоксы оказывались привязаны к внешней форме с непредсказуемыми последствиями.

Фикс: bulk-формы вынесены ИЗ карточек как пустые `<form hidden>` (только csrf-токен). Чекбоксы строк и bulk-кнопки в футере связаны с ними через HTML5 атрибут `form="..."`. Per-row формы стали плоскими.

## 2026-05-19 (поздний вечер) — `[feat]` Экспорт approved выводов: точный диапазон дат

К быстрым пресетам «Сегодня / Вчера / 7 дней» добавил **произвольный диапазон**: два date-input'а («С» / «по») + кнопка «📅 Скачать за диапазон».

- URL: `?date_from=YYYY-MM-DD&date_to=YYYY-MM-DD` (включительно, MSK)
- Диапазон имеет приоритет над `?period=` — если переданы обе даты, пресет игнорируется
- Авто-swap если `from > to`
- Заголовок в файле: «за DD.MM.YYYY» (если один день) или «за DD.MM.YYYY—DD.MM.YYYY»
- Имя файла: `withdrawals_approved_<from>_<to>_<today>.xlsx`

## 2026-05-19 (вечер) — `[feat]` Экспорт ОДОБРЕННЫХ выводов за период в Excel

На `/staff/withdrawal-requests/` карточка «📊 Экспорт одобренных выводов» с тремя кнопками:

- **Сегодня** — approved за текущий MSK-день (по `processed_at`)
- **Вчера** — approved за вчерашний день
- **Последние 7 дней** — включая сегодня

Колонки: ID, @username, ФИО (СМЗ), Аккредитован, Сумма ₽, Тип баланса (обычный/дожим), Реквизиты, Рефовод, Telegram ID, Дата подачи, Дата одобрения, Кто одобрил. Последней строкой — ИТОГО.

URL: `/staff/withdrawal-requests/export/?period=today|yesterday|7days`. Только `main_admin`. Имя файла: `withdrawals_approved_<period>_YYYY-MM-DD.xlsx`.

Изначально (короткий период до feedback'а) выгружались pending — переделано на approved, потому что для бухгалтерии важно «что реально выплатили», а не «что в очереди».

## 2026-05-19 — `[feat]` Календарь /staff/calendar/ исключает события Владимира

На windowgram появился второй профиль для Владимира — таблица `conversations` теперь хранит `last_profile = 'artem' / 'vladimir' / NULL`. У него отдельный календарь, а наш `/staff/calendar/` показывает только Артёма.

- В кросс-БД SQL добавил `AND (c.last_profile IS NULL OR c.last_profile <> 'vladimir')`. `NULL` остаётся видимым как legacy-данные до запуска фичи 18.05.
- Сейчас на проде: 206 событий (artem + legacy null) остаются, 2 события профиля `vladimir` скрыты.
- `_bookings_by_minute_window` (свободные слоты для менеджеров в /worker/*) **намеренно не фильтруется** — на боте `booking.py` считает capacity по всем событиям без разбора профиля, и одностороний фильтр у нас даст рассинхрон («у нас свободно — у бота занято»).

## 2026-05-15 (день) — Вкладка «🔍 Не в боте» теперь доступна всем модераторам

Раньше эта focused-вкладка была доступна только `main_admin` и задумывалась как «спецпроверка». На практике модераторам тоже удобно: один клик — и видишь только pending отчёты с ручной привязкой (manual_unverified=True), где нужно повнимательнее смотреть скриншот переписки.

- Сняли проверку `is_main_admin` в `admin_search_reports_list` и в шаблоне.
- Счётчик `not_in_bot_count` теперь считается для всех модераторов.
- В обычных вкладках (Новые/Доработка/…) такие отчёты по-прежнему видны вместе со всеми, с бейджем `🔍 Ручная`. Эта вкладка — просто фокус на них.

## 2026-05-15 — `[audit + fix]` Парсер ручного ID отвергает телефоны

Аудит «почему так много manual_unverified отчётов»:

- Проверка `_client_in_windowgram` корректно охватывает все 18 ботов на windowgram-сервере (`telegram_users` — глобальная таблица без `bot_id`).
- 73 уникальных username из 185 текущих manual_unverified — 0 совпадений в `telegram_users.username`, `first_name`, `conversations.last_profile`, `client_display_name`. То есть это реальные клиенты, писавшие менеджерам в ЛС, минуя бота. Алгоритм работает правильно.
- **Найден баг парсера:** `parse_manual_client_input` принимал любую цифровую строку как `telegram_id`. На проде нашли 5 отчётов, где менеджеры случайно вписали телефонные номера (`89041375610`, `89215707076`, и т.д.) в поле «ID клиента» — парсер сожрал, и отчёты ушли в `manual_unverified=True`. 4 из 5 уже approved.

Фикс:
- Новая эвристика `_looks_like_phone_number`: отвергает 11 цифр с префиксом 7/8, 10 цифр начинающиеся с 9, и любые 12+ цифр.
- В сообщении менеджеру явно подсказываем: «похоже на телефон, переключите тип отчёта на phone_callback».

## 2026-05-14 (поздний вечер) — `[fix]` 110 pending SR висели невидимыми

Жалоба от менеджеров и модератора Анастасии: «Бот запущен, а на проверке не видит». В реальности было следующее:

- На странице `admin_search_reports_list` обычные вкладки фильтровали `manual_unverified=False`. Изначально это задумывалось как safety net для случаев, когда менеджер вручную вписал ID клиента, а в БД бота клиента нет.
- На практике эта ручная привязка стала основным fallback'ом когда бот-deeplink не сработал (~95% pending'ов попадали именно туда).
- Главный админ видел их в отдельной вкладке «Не в боте», обычные модераторы — нигде. Отчёты копились по 110 шт, менеджеры жаловались что "леды исчезают".

Фикс:
- Убрал фильтр `manual_unverified=False` в обычных вкладках админа SR.
- Сейчас обычные модераторы видят все pending в одной куче, включая ручные привязки.
- Pending-каунтер: было ~20 → стало 131 (110 ранее невидимых разморозились).
- Вкладка «🔍 Не в боте» осталась как focused-фильтр только для main_admin.
- В таблице у manual_unverified отчёта появился бейдж `🔍 Ручная` (жёлтый, с тултипом).

## 2026-05-14 (вечер) — `[fix]` GroupReport approve не начислял баланс рефоводу/партнёру

Долго существовавший баг: при approve GR создавалась запись `PartnerEarning`, но сам `partner.balance` не обновлялся (в Lead/SR approve проводка была, в GR — забыли).

Симптом: рефовод/партнёр в кабинете видит «История начислений: +20 ₽», но в шапке «Баланс» сумма не растёт. Сейчас в кабинете `julia_lead` именно эта ситуация — за сегодня 5 × +20 = 100 ₽ начислений в журнале, баланс стоит как был.

- **Approve**: после `PartnerEarning.objects.create(...)` теперь идёт `select_for_update owner → balance += cut → log_balance_change` + авто-аккредитация если был минус.
- **Reject/Rework**: симметрично — снимаем сумму с баланса перед удалением PE.
- **Backfill 700 ₽** применён напрямую в БД: `Marinka_1998` +600, `julia_lead` +100, с записями в balance_log.

## 2026-05-14 (вечер) — `/referrals/`: фильтр «только активные» по умолчанию

По просьбе рефоводов: при сотнях зарегистрировавшихся реально работают единицы, и просматривать 3 страницы со «спящими» рефералами — неудобно.

- На страницах `/referrals/` и `/referrals/list/` теперь по умолчанию показываются только **активные** рефералы (с ≥1 одобренным отчётом Lead/SearchReport/GroupReport).
- В шапке счётчик «активных X из Y». Кнопка переключения «Только активные / Все (Y)».
- Параметр URL: `?show=active` (по умолчанию) или `?show=all`. Пагинация сохраняет фильтр.
- Если активных нет, но рефералы есть — показывается дружелюбное «Нет активных рефералов. Всего: N. Показать всех →».
- На примере одного из рефоводов: было 126 рефералов → стало 37 активных в списке.

## 2026-05-14 — Sub-рефовод milestone: 500 ₽ за 10 одобренных отчётов реферала

### Новая модель мотивации для рефералов главных рефоводов
- Для пользователей `role=user`, у которых есть свой `partner_owner` (т.е. они сами пришли по чьей-то реф-ссылке), вместо общей % схемы вводится **одноразовый бонус 500 ₽** за каждого реферала, который сдал **10 одобренных отчётов** (Lead + SearchLink + GroupReport суммарно).
- Регулярная % схема (cut от пула за каждый отчёт) у таких sub-рефоводов **отключена** — реф получает полную ставку за каждый отчёт.
- Главных рефоводов (`partner_owner IS NULL`) и партнёров (`role=partner`) изменения не затрагивают.

### Реализация
- `User.subref_bonus_paid_at` (DateTimeField, nullable) — флаг идемпотентности выплаты.
- `core/lead_utils.py`: `is_subreferrer()`, `check_and_pay_subref_milestone()` — вызывается из всех трёх approve-вьюх (Lead, SearchReport, GroupReport) после стандартного начисления.
- Lead/SR/GR approve: при `is_subreferrer(owner)` пропускают создание `PartnerEarning` и оставляют полную ставку рефу; затем вызывают milestone-helper.
- Reject/rework existing-логика без изменений: для sub-рефовода `PartnerEarning` не создавался — откатывать нечего. Сам milestone-бонус не клавбачится (one-time дизайн).
- Миграции: 0077 — stub после revert (поле `ref_bonus_percent` уже применилось на проде), 0078 — `RemoveField(ref_bonus_percent) + AddField(subref_bonus_paid_at)`.

### UI
- `/referrals/` и `/referrals/list/`: для sub-рефоводов вместо разбивки «Lead / SearchLink / Группы» показывается **прогресс-бар X/10** по каждому рефералу + бейдж «+500 ₽ выплачено» когда milestone достигнут.
- Форма редактирования общих ставок (SearchLink cut, GroupReport cut) для sub-рефоводов скрыта — заменена на info-блок с описанием «Приведи 10 друзей».

## 2026-05-06 (ночь) — GroupReport (бета): отчёты менеджеров по группам

### Новый поток отчётов
- Модель `GroupReport` (миграция `0065_group_reports`): user, platform (TG/VK), client_platform_id + client_username, manager_platform_id + manager_username, report_date, screencast (FileField, до 50 МБ), status (pending/approved/rejected/rework), is_complete, validation_note, paid_reward.
- Право `User.can_create_group_reports` (BooleanField). Без него — никаких новых кнопок у менеджера.

### Авто-валидация против бот-сервера
- При сабмите отчёта Django запрашивает таблицу `windowgram.admin_task_progress` по связке `(platform, admin_platform_user_id, client_platform_user_id)`.
- Если запись найдена и все 4 этапа выполнены (`artem_invited + link_done + offer_done + sozvon_done`) — `is_complete=True` и отчёт виден всем модераторам.
- Иначе `is_complete=False` + `validation_note` («Артём не приглашён», «/линк не выполнен» и т.п.) — отчёт виден **только главному админу** во вкладке «⚠ Не полные».

### Главный админ — отдельная карточка прав
- Дашборд `dashboard_main_admin.html`: новая зелёная карточка «⚙ Права на отчёты» (`/staff/group-report-permissions/`).
- На странице — поиск активных юзеров (role=user, не банненые) + grant/revoke со счётчиком текущих обладателей права.

### Модерация (admin + main_admin)
- `/staff/group-reports/` — список с фильтрами: pending / rework / approved / rejected. Главному админу — ещё «⚠ Не полные».
- Approve начисляет менеджеру **200 ₽** (`GROUP_REPORT_APPROVE_REWARD`) через `log_balance_change` — как у Lead/SearchReport.
- Reject/rework с причиной; если был approved — баланс автоматически откатывается.
- Обычный admin физически не видит и не может оперировать `is_complete=False` (даже по прямой ссылке — 403).

### Кабинет менеджера
- Дашборд `dashboard.html`: при `can_create_group_reports=True` появляются две новые карточки:
  - «📊 Отчёты по группам (бета)» — список своих отчётов и кнопка «＋ Новый» (`/reports/groups/`, `/reports/groups/create/`).
  - «📅 Свободные слоты» (только если `status=approved`) — `/calendar/free-slots/`.
- Бейдж «N на доработке» если у менеджера есть rework-отчёты.

### Урезанный календарь свободных слотов
- `free_slots_calendar` — копия логики из `windowgram.api.booking` (SCHEDULE / SCHEDULE_OVERRIDES / SLOT_STEP=15 / MIN_GAP=15 / CAPACITY=2 / HORIZON=14 дней).
- Менеджер видит только сетку свободных/занятых слотов **без имён клиентов и любых деталей**. Полный календарь у админа остаётся как есть.
- Если расписание на боте поменяется — обновить константы в `core/views_group_reports.py` (отмечено в комментарии).

### Файлы
- `core/views_group_reports.py` — новый модуль (~470 строк)
- `core/migrations/0065_group_reports.py` — миграция
- 7 новых шаблонов в `templates/core/`
- 13 новых URL в `core/urls.py`
- Карточки в `dashboard.html`, `dashboard_main_admin.html`, `dashboard_admin.html`

## 2026-05-06 (вечер) — календарь UX-улучшения + перенос в дашборд

### Перенос
- Кнопка «Календарь» убрана из навбара, добавлена карточкой в раздел «Управление» дашборда главного админа (`templates/core/dashboard_main_admin.html`).
- На карточке — счётчик «N сегодня» (запрос в `windowgram` обёрнут в try/except, при недоступности БД счётчика не будет, но дашборд не сломается).

### Календарь стал читаемым
- **Сегодня** — большой синий круг вокруг номера дня + подсветка фона ячейки. Видно сразу.
- **Лимит 4 события на ячейку**, остальные собраны в кнопку «+ ещё N» — клик открывает модалку дня со ВСЕМИ событиями этого дня.
- **Цвета по времени**: утро (до 12) — голубой, день (12–18) — фиолетовый, вечер (18+) — золотой, без времени — серый, статусные напоминалки — розовый.
- **Прошедшие дни** приглушены, выходные слегка золотистые.
- **Модалки на день** вместо per-event — было 229 DOM-нод модалок, стало ~30. Список событий внутри удобный (имя, время, ник, группа, статус, бот в одной строке).
- **Шапка** — без эмоджи `📅` (на macOS рендерилась криво как «JUL 17»). Под заголовком — subtitle «Май 2026 · 27 событий».

### Новый блок «Ближайшие 7 дней»
- Под сеткой — список из 7 ближайших дней с группировкой («Сегодня / Завтра / 8 мая, четверг»). Для каждого дня — счётчик событий и до 8 цветных бейджей. Удобно сканировать «что у меня на этой неделе» без поиска по сетке.
- Клик на любую плашку → та же модалка дня.

## 2026-05-06 — календарь созвонов в админке (read-only зеркало мурзвона)

### Главному админу теперь доступен календарь созвонов прямо у нас
- Новый эндпоинт `/staff/calendar/` (`name='admin_calendar'`) — месячная сетка созвонов из БД бота (`windowgram.calendar_events`). Только для `role='main_admin'`.
- Чтение — raw SQL через уже существующий коннект `connections["windowgram"]` (тот же, что используется в `auto_match_searchlinks_with_bot_convs`). Никакого CRUD, никаких изменений на стороне FastAPI/мурзвона.
- JOIN на `conversations + telegram_users + bots`: показываем имя клиента, username/vk_id, группу, бота, статус диалога. Платформа определяется по `vk_peer_id`.
- Статусные напоминалки скрыты по умолчанию (`[Жду бабки]`, `[Ответ]`, `[Жду без даты]`, `[Просрочка]`) — переключатель `?all=1` их показывает в оранжевом цвете.
- Навигация по месяцам через `?ym=YYYY-MM`, кнопка «Сегодня». Сетка 7×6, выходные подсвечены.
- Клик на событие → Bootstrap-модалка с деталями (дата+время, ник, группа, статус, бот, время создания записи).
- Кнопка «📅 Календарь» в навбаре `templates/base.html` — видна только для `role='main_admin'`.

### Подготовка к будущему расширению
- Та же инфраструктура (`connections["windowgram"]`) пригодится, чтобы потом подтверждать SearchReport'ы по реальным статусам созвонов из таблицы `call_events`.
- Локальный `.env` дополнен опциональными `WG_DB_*` (не уходят в git). На App Platform коннект работает по fallback на `DB_*` — windowgram и default_db на одном PostgreSQL-кластере.

## 2026-04-27 (ночь) — авто-матчинг SearchLink с базой бота

### Авто-подтверждение старта бота через cross-DB матчинг
- **Проблема**: webhook от бота `/start <code>` не всегда долетает до сайта (Telegram съедает deeplink-параметр на каких-то клиентах). Менеджеры жалуются: «клиент стартовал, а на сайте Ожидание»
- **Фикс**: при каждом cron-проходе (раз в минуту, через `/api/cron/poll-incoming-calls/`) сайт теперь дополнительно проверяет windowgram.conversations против unstarted SearchLink'ов
- **Логика консервативного матча**: для каждого нового conv в боте B ищем единственную unstarted-ссылку этого бота, созданную в окне [conv.created_at - 6h, conv.created_at]. Если ровно одна — матчим (ставим `bot_started=true`, `telegram_id`, `telegram_username`). Если ноль или две — пропускаем (ambiguous)
- **Защита от дубля**: telegram_id уже привязанные к другим SL за 24h не претендуют на повторный матч
- Добавлена БД `windowgram` в `DATABASES` (read-only, через router запрещены миграции)
- Mgmt-команда `python manage.py match_searchlinks` для локального тестирования

### Ручное подтверждение остаётся — но как fallback
- Кнопка «✓ Подтвердить» в `my_links` для admin/support/main_admin сохраняется (для случаев когда даже автомэтч не справился)

## 2026-04-27 (поздно вечер) — UX-фиксы

### Кнопки «Отменить / Доработка» на одобренных лидах вернулись в видимое место
- В `admin_leads_all_new.html` для `tab='all'` кнопки теперь рендерятся **inline рядом с бейджем «Одобрен»** (раньше были в столбце «Действия» в самом конце таблицы — на узких экранах админ их не видел)
- Серверная логика `admin_lead_reject` / `admin_lead_rework` уже поддерживала отмену одобренных (rollback баланса) — никаких изменений в view

### Ручное подтверждение старта бота (для админов)
- На странице `my_links.html` рядом с бейджем «Ожидание» админам/саппорту/main_admin показывается кнопка «✓ Подтвердить»
- При клике — prompt на @username клиента (для аудита) + confirm → POST `/search/links/<code>/force-confirm-start/` → `bot_started=true`, `bot_started_at=now()`, опционально `telegram_username`
- Используется когда клиент стартовал бот, но webhook не сработал (Telegram обрезал deep-link param на каком-то клиенте, или клиент стартанул вручную без deep-link). Логируется в журнал
- Менеджерам кнопка не видна (защита от накрутки)

## 2026-04-27 (вечер) — переход phone_callback на pull-модель

### Полный реверс системы звонков: теперь не мы звоним клиенту, а клиент звонит нам
- Менеджер выбирает в форме отчёта тип «📞 Клиент позвонит», вводит номер клиента и получает 5 наших номеров для передачи: +7 965 362-14-16 / +7 925 205-81-93 / +7 915 419-48-57 / +7 991 882-64-08 / +7 933 111-83-35
- Клиент звонит на любой из этих номеров (привязаны к входящей кампании zvonok ID 1738255164), нажимает «1» в IVR
- Раз в час бот-сервер дёргает `/api/cron/poll-incoming-calls/?secret=...` — поллер проходит по всем `pending_callback` отчётам и опрашивает zvonok через `GET /phones/calls_by_phone/`
- Если найден звонок с нажатой «1» — `callback_confirmed_at = now`, статус → `pending` (на проверку админу)
- Throttle: каждый отчёт опрашивается не чаще раза в 55 минут, batch ≤ 200 за прогон
- Бета-гейт `PHONE_CALLBACK_BETA_USERNAMES` снят — phone_callback доступен всем менеджерам

### Удалено
- Исходящие robocalls (3 stage'а: сразу / -1ч / -10мин)
- `RobocallAttempt` модель не используется (но таблица оставлена ради исторических данных)
- View `zvonok_webhook` (вебхук от zvonok), `zvonok_dispatch_cron` (диспатч исходящих)
- Routes `/api/zvonok-callback/`, `/api/cron/dispatch-robocalls/`
- Поля формы «Дата созвона / Время созвона»
- Mgmt command `dispatch_robocalls` → переименована в `poll_incoming_calls`

### Новые поля БД
- `SiteSettings.zvonok_incoming_campaign_id` (default `1738255164`)
- `SearchReport.zvonok_last_polled_at` (datetime, indexed) — для throttle поллера
- `SearchReport.zvonok_call_id` (varchar 64) — какой звонок подтвердил отчёт
- Старые `zvonok_campaign_id_now/_1h/_10min` оставлены в БД до отдельной cleanup-миграции (для безопасного автодеплоя)

## 2026-04-27

### Аккредитованные пользователи: безлимит на выдачу контактов
- Пользователи с `is_accredited=True` (галочка) теперь могут брать контакты без дневного лимита и без заявки админу
- За один клик «Получить контакты» выдаётся пачка `base_limit` (дефолтный лимит базы), повторные клики — без ограничений
- Если в базе свободных контактов меньше, чем `base_limit` — выдаём что есть, а не блокируем (для не-аккредитованных поведение прежнее: «недостаточно контактов»)
- На странице `/contacts/` для аккредитованных показана подсказка про безлимит, кнопка «Обратиться» при `not_enough` скрыта

## 2026-04-21

### Баг: бейдж «Одобрен +N₽» на SearchLink показывал неверную сумму
- Причина: шаблон рендерил `+{{ search_reward }}₽`, а `search_reward` вычислялся динамически от ТЕКУЩЕГО `SEARCH_REPORT_REWARD`. После смены ставки 100→150 бейджи старых одобрений начали показывать новую сумму, хотя на баланс пошла старая.
- Фикс: добавлено поле `SearchReport.paid_reward` (миграция 0051) — фиксирует реально начисленную менеджеру сумму на момент одобрения
- Бэкфилл: paid_reward для 46 исторических одобрений выдернут из `core_balancelog` по reason `search_approve#<id>`
- Шаблон `my_links.html` теперь берёт `report.paid_reward` (fallback на `search_reward` только для записей без бэкфилла)
- Вьюха `admin_search_report_approve` при одобрении пишет фактическую сумму в `paid_reward`
- Компенсация для pip1au7: +50₽ (баланс 90→140), `paid_reward` на report #94 обновлён до 120₽
- Массовая компенсация остальным 45 отчётам за 20.04 (одобрены до деплоя ставки 150): +50₽ менеджеру по каждому отчёту, `paid_reward` +50 в каждом, запись в `core_balancelog` с reason `searchlink_rate_compensation#<report_id>`. Итого 2250₽ на 11 менеджеров. После компенсации все 46 одобрений 20.04 суммарно дают 150₽ (manager paid + partner cut)

## 2026-04-20

### SearchLink: выплата 100 → 150 ₽
- `SEARCH_REPORT_REWARD` в `base_site/settings.py` изменена с 100 на 150
- Применяется ко всем НОВЫМ одобрениям. Уже одобренные отчёты не пересчитываются
- Реферальная экономика: партнёр получает тот же `ref_searchlink_manager_cut`, менеджер получает `150 - cut` (было `100 - cut`)
- Все шаблоны и виды читают значение динамически — отобразится сразу после деплоя

### Дедлайн обычных отчётов
- На странице отправки отчёта `/leads/report/` добавлена красная плашка с предупреждением: отчёты принимаются до четверга 23.04 включительно

### Доработка лида — смена категории и замена вложения
- На странице доработки лида (`/leads/<id>/redo/`) добавлен select «Категория лида» с текущим значением выбранным
- Подсказка «Если при отправке выбрали не ту категорию — исправьте здесь»
- Явный индикатор текущего вложения: «🎬 Сейчас у лида видео» / «🖼 Сейчас у лида фото»
- Явная подсказка что можно заменить фото на видео и наоборот
- В плашке «Что нужно доработать» показывается @username админа, который кинул на доработку
- Расширен список разрешённых расширений до `_ATTACHMENT_ACCEPT` (mkv, avi, hevc и пр.)

### Модерация по админам
- Новая страница `/staff/moderation-by-admin/` — сводная таблица: кто из админов что принял/отклонил/отправил на доработку (по лидам и по SearchReport отдельно)
- Детализация `/staff/moderation-by-admin/<admin_id>/` — вкладки по статусам, переключатель «Обычные лиды / SearchLink отчёты», столбец «Причина / Что доработать»
- В таблицах лидов и отчётов `@reviewed_by` теперь кликабельная ссылка на детализацию админа
- В `admin_search_reports.html` «Проверил» теперь показывается и в rework-вкладке (раньше было только в approved/rejected)
- Ссылки в дашборды admin и main_admin

### SearchLink для админов
- `admin` и `main_admin` теперь могут создавать и вести свои SearchLink ссылки (кнопка на дашборде)
- Гейт `ref_searchlink_enabled` действует только на `role=user`

### SearchLink: telegram_username лида
- Добавлены поля `telegram_username`, `telegram_first_name` на `SearchLink` (миграция 0050)
- Бот при `/start` шлёт в вебхук `telegram_username` и `telegram_first_name` (патч на 72.56.24.65)
- В админке «Отчёты SearchLink», своих ссылках менеджера и статистике теперь показывается `@username` лида (с кликабельной ссылкой на t.me), а введённое менеджером имя — вторым рядом
- Если у клиента нет username в Telegram — фолбэк на старое `lead_name`
- Старые записи (до патча) остаются с пустым `telegram_username` — показываются по-старому

## 2026-04-17

### Баг: плашка «Заявка отклонена» на дашборде
- Плашка показывалась даже когда у юзера была более новая pending/approved заявка
- Причина: `last_rejected_wr` брал самый последний reject без проверки, была ли после него ещё одна заявка
- Фикс: теперь берётся ПОСЛЕДНЯЯ заявка юзера (любого статуса), и плашка показывается только если её статус = rejected

### Лендинг (главная)
- Бейдж «Работа с рассылками онлайн» → «Онлайн»
- Заголовок: «Зарабатывайте на рассылках…» → «Зарабатывайте онлайн — всё в одном кабинете»
- Убран абзац про базы/рассылки/отчёты/вывод
- Убрана подпись «После регистрации… доступ к базам откроется»
- Убрана правая карточка «Как это работает» с буллетами про базы и отчёты
- Лейаут: левая колонка стала на всю ширину (`col-lg-8`), центрирование

## 2026-04-05

### Аккредитация
- Автоматическая аккредитация: баланс из минуса → в плюс = галочка ✓
- Ручная аккредитация — только main_admin
- Галочка видна в списке пользователей и в выводах

### Выводы — разделение
- Страница выводов разделена на «Обычные пользователи» и «Рефералы»
- У рефералов колонка «Рефовод»
- Колонка аккредитации в обоих таблицах

## 2026-04-03

### Баг-фиксы
- Рефы видят реальную ставку (+25₽) вместо +40₽ в «Мои лиды»
- Missing import Count — 500 на main_admin дашборде
- Баланс-админ: убрана колонка «действий» (показывала чужие лиды)

### Партнёр: проверка дожим-лидов
- Nastya_Partner проверяет дожим-лиды своих рефов на `/partner/dozhim/leads/`
- Дожим-лиды рефов Насти (id=260) исключены из обычной админ-панели
- Бейдж pending на дашборде партнёра

## 2026-04-02

### BalanceLog — аудит баланса
- Новая таблица `core_balancelog` — запись каждого изменения баланса
- Покрывает: одобрение/reject/rework лидов, выводы, payment, партнёрские начисления
- Поля: user, field, old_value, new_value, delta, reason, actor, created_at

### Баг-фикс: max(0) убран
- Убран `max(0, ...)` из 12 мест — минусовые балансы больше не сбрасываются в 0

## 2026-04-01

### Реферальная система — нативная для всех менеджеров
- Роль `affiliate` удалена
- Каждый менеджер (role=user) имеет вкладку «Реферальная система» на дашборде
- `/referrals/` — создание ссылок с ref_reward (1-39₽)
- Экономика: реф получает ref_reward, менеджер получает 40 - ref_reward
- Роль partner (Настя, Юля) не тронута

### Страница управления ролями — удалена
- `/staff/roles/` удалён вместе с affiliate

## 2026-03-31

### Разделение балансов: Поиск и Дожим
- Новое поле `dozhim_balance` на User
- Навбар и дашборд показывают баланс текущего отдела
- Раздельный вывод средств с пометкой [Поиск]/[Дожим]
- Polling API обновляет правильный баланс

### Произвольная сумма вывода
- Все пользователи могут указать сумму (не обязательно весь баланс)
- Валидация: min ≤ amount ≤ balance

### Плашка вывода на дашборде
- «X руб. находится на выводе. Вывод будет выполнен в течение 3 дней»
- Для всех ролей: user, partner, affiliate, worker

### Баны
- Кнопка забанить/разбанить в списке пользователей
- Доступ: все админы + баланс-админ

### Оплата (баланс-админ)
- `/staff/payment/` — список пользователей, ×1.5, −25000, вернуть

## 2026-03-30

### Affiliate партнёрка (позже удалена)
- Роль affiliate с настраиваемой ставкой
- Дашборд, ссылки, рефералы, вывод

### SearchLink система (100₽ за лид)
- Менеджер создаёт ссылку → standalone лендинг `/s/<code>/`
- 16 ботов в пуле, deep link, OG мета-теги с ФИО
- Вебхук от WindowGram ботов → деактивация страницы
- Отчёты видны админу только если бот стартован
- Одобрение = +100₽

### Дожим разблокирован
- Убран lock (user id=206) — все пользователи могут работать с дожимом
- Вознаграждение: 30₽ → 40₽

### Навбар и регистрация
- Скрыты кнопки «Войти/Регистрация» на реф-страницах
- Убрана «Уже есть аккаунт? Войти» с реф-страниц

### Поиск и пагинация
- Мои лиды, задания воркера, self-leads — поиск + пагинация

### Воркеры
- Кнопка «Отказались, связаться Артёму»
- Вкладка «Отказы» у СС-админа
